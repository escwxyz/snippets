{
  "Vitest source test": {
    "prefix": "vitest",
    "body": [
      "if (import.meta.vitest) {",
      "\tconst {test, expect} = import.meta.vitest",
      "\ttest('test one', () => {",
      "\t\texpect().toBe()",
      "\t}",
      "}"
    ],
    "description": "Vitest source test"
  }
}
